# this is necessary because readthedocs does not have gobject-introspection-1.0
# installed in their virtual machines, so the normal install mechanism that uses 
# setup.py does not work, it fails when trying to import pygobject.
appdirs>=1.4.3
arrow>=0.15.5
cffi>=1.13.2
cryptography>=2.8
docopt>=0.6.2
inform>=1.20.0
pycairo>=1.19.0
pycparser>=2.19
pyotp>=2.3.0
python-dateutil>=2.8.1
python-gnupg>=0.4.5
six>=1.14.0
avendesora>=1.16
