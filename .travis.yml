language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "nightly"
addons:
  apt:
    packages:
      - gnupg
install:
  - pip install .
  - chmod -R go-rwx tests/home/gnupg
script: 
  - cd tests; py.test
deploy:
    provider: pypi
    user: avendesora
    on:
        tags: true
